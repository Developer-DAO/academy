import Head from 'next/head'
import Link from 'next/link'
import { Heading, Flex, Stack, Divider, Button, Text } from '@chakra-ui/react'
import fs from 'fs'
import path from 'path'
import matter from 'gray-matter'
import { ContentBanner } from '../../components/ContentBanner'

interface LessonProps {
  lessons: {
    frontMatter: any
    slug: string
  }[]
}

const GettingStarted: React.FC<LessonProps> = ({ lessons }) => {
  return (
    <>
      <Head>
        <title>D_D School of Code</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Flex as="main" py={5} px={[4, 10, 16]} direction="column" minH="100vh">
        <Stack spacing={5} direction="column">
          <Heading
            as="h2"
            fontSize="3xl"
            textAlign="center"
            color="#F96C9D"
            apply="mdx.h2"
          >
            GETTING STARTED
          </Heading>
          <Divider />
          <Heading apply="mdx.h3" as="h3" fontSize="2xl" textAlign="center">
            How it started (Introduction to project)
          </Heading>
          <Divider />
          <Heading apply="mdx.h3" as="h3" fontSize="2xl" textAlign="center">
            Highlights of Resources
          </Heading>
          <Divider />
          <Heading apply="mdx.h3" as="h3" fontSize="2xl" textAlign="center">
            Roadmap
          </Heading>
          <Divider />
          <Heading apply="mdx.h3" as="h3" fontSize="2xl" textAlign="center">
            How to send feedback
          </Heading>
          <Divider />
          <Heading apply="mdx.h3" as="h3" fontSize="2xl" textAlign="center">
            Current Lessons
          </Heading>
          <Divider />
          {lessons.map((lesson: any, idx: number) => (
            <Link key={lesson.slug} href={'/lessons/' + lesson.slug} passHref>
              {/* <ContentBanner lesson={lesson} idx={idx} /> */}
              <Button>{lesson.frontMatter.title}</Button>
            </Link>
          ))}
        </Stack>
      </Flex>
    </>
  )
}

export default GettingStarted

export const getStaticProps = async () => {
  const files = fs.readdirSync(path.join('lessons'))
  const lessons = files.map((filename) => {
    const markdownWithMeta = fs.readFileSync(
      path.join('lessons', filename),
      'utf-8',
    )
    const { data: frontMatter } = matter(markdownWithMeta)
    return {
      frontMatter,
      slug: filename.split('.')[0],
    }
  })
  return {
    props: {
      lessons,
    },
  }
}
